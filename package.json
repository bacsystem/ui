{
  "name": "bacsystem-ui-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @bacsystem/demo",
    "build": "npm run build -w @bacsystem/ui",
    "build:ui": "npm run build -w @bacsystem/ui",
    "dev:ui": "npm run dev -w @bacsystem/ui",
    "typecheck": "npm run typecheck -w @bacsystem/ui"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
